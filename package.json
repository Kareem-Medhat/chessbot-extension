{
  "name": "chessbot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "content": "esbuild --bundle src/content.ts --outfile=dist/content.js --minify",
    "background": "esbuild --bundle src/background.ts --outfile=dist/background.js --minify --format=esm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.7",
    "chokidar": "^3.6.0",
    "chrome-types": "^0.1.194",
    "esbuild": "^0.21.3"
  }
}